<Page x:Class="WpfSUB.Pages.SubscriptionPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Подписки">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Панель инструментов -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <Button Content="Назад" Click="Back_Click" Margin="5"/>
            <Button Content="Добавить" Click="Add_Click" Margin="5"/>
            <Button Content="Редактировать" Click="Edit_Click" Margin="5" x:Name="EditButton"/>
            <Button Content="Удалить" Click="Delete_Click" Margin="5" x:Name="DeleteButton"/>
            <Button Content="Обновить" Click="Refresh_Click" Margin="5"/>
        </StackPanel>

        <!-- Список подписок -->
        <ListView Grid.Row="1" Margin="10" x:Name="SubscriptionsListView" SelectionChanged="SubscriptionsListView_SelectionChanged">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" Width="50" DisplayMemberBinding="{Binding Id}"/>
                    <GridViewColumn Header="Клиент" Width="150" DisplayMemberBinding="{Binding Client.FullName}"/>
                    <GridViewColumn Header="Издание" Width="150" DisplayMemberBinding="{Binding Publication.Title}"/>
                    <GridViewColumn Header="Период" Width="80" DisplayMemberBinding="{Binding PeriodMonths, StringFormat={}{0} мес.}"/>
                    <GridViewColumn Header="Сумма" Width="100" 
                                  DisplayMemberBinding="{Binding TotalPrice, StringFormat={}{0:C}}"/>
                    <GridViewColumn Header="Статус" Width="100" DisplayMemberBinding="{Binding Status}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>