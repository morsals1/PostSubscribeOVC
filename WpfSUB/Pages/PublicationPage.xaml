<Page x:Class="WpfSUB.Pages.PublicationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Издания">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <Button Content="Назад" Click="Back_Click" Margin="5" Width="80"/>
            <Button Content="Добавить" Click="Add_Click" Margin="5" Width="80"/>
            <Button Content="Редактировать" Click="Edit_Click" Margin="5" Width="110" x:Name="EditButton"/>
            <Button Content="Удалить" Click="Delete_Click" Margin="5" Width="80" x:Name="DeleteButton"/>
            <Button Content="Обновить" Click="Refresh_Click" Margin="5" Width="80"/>
            <Button Content="Экспорт" Click="Export_Click" Margin="5" Width="80"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10,0,10,10">
            <TextBlock Text="Поиск:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox x:Name="SearchTextBox" Width="250" TextChanged="SearchTextBox_TextChanged" Margin="0,0,20,0"/>
            <Button Content="Сменить доступность" Click="ToggleAvailability_Click" Margin="5" Width="150" x:Name="ToggleAvailabilityButton"/>
        </StackPanel>
        <Border Grid.Row="2" Background="#F5F5F5" Padding="10" Margin="10,0,10,10">
            <TextBlock x:Name="StatsTextBlock" Text="" TextWrapping="Wrap"/>
        </Border>
        <ListView Grid.Row="3" Margin="10" x:Name="PublicationsListView" SelectionChanged="PublicationsListView_SelectionChanged">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" Width="40" DisplayMemberBinding="{Binding Id}"/>
                    <GridViewColumn Header="Название" Width="180">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Title}" TextWrapping="Wrap" MaxWidth="180"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Категория" Width="110" DisplayMemberBinding="{Binding Category.Name}"/>
                    <GridViewColumn Header="Издатель" Width="130" DisplayMemberBinding="{Binding Publisher}"/>
                    <GridViewColumn Header="ISSN" Width="90" DisplayMemberBinding="{Binding ISSN}"/>
                    <GridViewColumn Header="Период." Width="70" DisplayMemberBinding="{Binding Periodicity}"/>
                    <GridViewColumn Header="Цена/мес" Width="70" 
                                  DisplayMemberBinding="{Binding MonthlyPrice, StringFormat={}{0:C}}"/>
                    <GridViewColumn Header="Доступно" Width="70">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsAvailable}" IsEnabled="False"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Подписок" Width="70" DisplayMemberBinding="{Binding Subscriptions.Count}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>