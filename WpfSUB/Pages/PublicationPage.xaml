<Page x:Class="WpfSUB.Pages.PublicationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Издания">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Панель инструментов -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <Button Content="Назад" Click="Back_Click" Margin="5"/>
            <Button Content="Добавить" Click="Add_Click" Margin="5"/>
            <Button Content="Редактировать" Click="Edit_Click" Margin="5" x:Name="EditButton"/>
            <Button Content="Удалить" Click="Delete_Click" Margin="5" x:Name="DeleteButton"/>
            <Button Content="Обновить" Click="Refresh_Click" Margin="5"/>
        </StackPanel>

        <!-- Список изданий -->
        <ListView Grid.Row="1" Margin="10" x:Name="PublicationsListView" SelectionChanged="PublicationsListView_SelectionChanged">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" Width="50" DisplayMemberBinding="{Binding Id}"/>
                    <GridViewColumn Header="Название" Width="250" DisplayMemberBinding="{Binding Title}"/>
                    <GridViewColumn Header="Издатель" Width="150" DisplayMemberBinding="{Binding Publisher}"/>
                    <GridViewColumn Header="ISSN" Width="100" DisplayMemberBinding="{Binding ISSN}"/>
                    <GridViewColumn Header="Цена/мес" Width="80" 
                                  DisplayMemberBinding="{Binding MonthlyPrice, StringFormat={}{0:C}}"/>
                    <GridViewColumn Header="Доступно" Width="80">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsAvailable}" IsEnabled="False"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>