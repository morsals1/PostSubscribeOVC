<Page x:Class="WpfSUB.Pages.ClientPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Клиенты">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <Button Content="Назад" Click="Back_Click" Margin="5" Width="80"/>
            <Button Content="Добавить" Click="Add_Click" Margin="5" Width="80"/>
            <Button Content="Редактировать" Click="Edit_Click" Margin="5" Width="110" x:Name="EditButton"/>
            <Button Content="Удалить" Click="Delete_Click" Margin="5" Width="80" x:Name="DeleteButton"/>
            <Button Content="Обновить" Click="Refresh_Click" Margin="5" Width="80"/>
            <Button Content="Экспорт" Click="Export_Click" Margin="5" Width="80"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10,0,10,10">
            <TextBlock Text="Поиск:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox x:Name="SearchTextBox" Width="200" TextChanged="SearchTextBox_TextChanged" Margin="0,0,20,0"/>
            <Button Content="Просмотреть подписки" Click="ViewSubscriptions_Click" Margin="5" Width="150" x:Name="ViewSubscriptionsButton"/>
            <Button Content="Оформить подписку" Click="CreateSubscription_Click" Margin="5" Width="150" x:Name="CreateSubscriptionButton"/>
        </StackPanel>
        <Border Grid.Row="2" Background="#F5F5F5" Padding="10" Margin="10,0,10,10">
            <TextBlock x:Name="StatsTextBlock" Text="" TextWrapping="Wrap"/>
        </Border>
        <ListView Grid.Row="3" Margin="10" x:Name="ClientsListView" SelectionChanged="ClientsListView_SelectionChanged">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" Width="40" DisplayMemberBinding="{Binding Id}"/>
                    <GridViewColumn Header="ФИО" Width="180" DisplayMemberBinding="{Binding FullName}"/>
                    <GridViewColumn Header="Адрес" Width="180">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Address}" TextWrapping="Wrap" MaxWidth="180"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Телефон" Width="100" DisplayMemberBinding="{Binding Phone}"/>
                    <GridViewColumn Header="Email" Width="160" DisplayMemberBinding="{Binding Email}"/>
                    <GridViewColumn Header="Паспорт" Width="100">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock>
                                    <Run Text="{Binding PassportSeries}"/>
                                    <Run Text=" "/>
                                    <Run Text="{Binding PassportNumber}"/>
                                </TextBlock>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Дата регистрации" Width="110" 
                                  DisplayMemberBinding="{Binding RegistrationDate, StringFormat=dd.MM.yyyy}"/>
                    <GridViewColumn Header="Подписок" Width="70" DisplayMemberBinding="{Binding Subscriptions.Count}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>