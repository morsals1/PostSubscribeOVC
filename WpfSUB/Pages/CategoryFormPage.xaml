<Page x:Class="WpfSUB.Pages.CategoryFormPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Новая категория">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Форма категории" 
                   FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1">
            <Label>Название категории:</Label>
            <TextBox x:Name="NameTextBox" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" 
                     Margin="0,0,0,10"/>

            <Button Content="Сгенерировать название" Click="GenerateName_Click" 
                    HorizontalAlignment="Left" Width="180" Margin="0,0,0,20"/>

            <TextBlock Text="Примеры категорий:" FontStyle="Italic" Margin="0,0,0,5"/>
            <TextBlock Text="Газеты, Журналы, Научные издания, Технические издания, Детские издания, Спортивные издания" 
                       TextWrapping="Wrap" Foreground="Gray"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="SaveButton" Content="Сохранить" Click="Save_Click" 
                    Width="100" Margin="5" FontWeight="Bold"/>
            <Button Content="Отмена" Click="Cancel_Click" Width="100" Margin="5"/>
        </StackPanel>
    </Grid>
</Page>